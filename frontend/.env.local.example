# TinyGuide AI — frontend environment template.
# Copy to `.env.local`. Exposed to the browser, so only put public values here.

# Base URL of the FastAPI backend microservice.
NEXT_PUBLIC_API_URL=http://localhost:8000
