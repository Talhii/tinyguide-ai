# TinyGuide AI — Backend dependencies (Python 3.11+)

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Data validation / settings
pydantic==2.10.3
pydantic-settings==2.6.1

# Database / auth (Supabase)
supabase==2.10.0

# AI orchestration
# langchain-community powers the RAG retrieval layer (loaders + vector stores).
# Generation uses Groq (free) by default, with Claude as an optional provider.
langchain-community==0.3.9
groq==1.5.0
anthropic==0.40.0

# Environment file support
python-dotenv==1.0.1
